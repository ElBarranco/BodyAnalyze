import SwiftUI

struct AllRunningSessionsView: View {
    var body: some View {
        NavigationView {
            Text("📂 Liste des sessions à venir")
                .navigationTitle("Toutes les sessions")
        }
    }
}
